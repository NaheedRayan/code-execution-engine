{
  "name": "server",
  "version": "1.0.0",
  "description": "a nodejs(express) server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.7.1",
    "express": "^4.17.1",
    "redis": "^3.1.0"
  }
}
